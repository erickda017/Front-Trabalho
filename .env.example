VITE_API_URL=http://localhost:3333/api

# Mesmos dados do backend (Project Settings > API no Supabase).
# A anon key é pública por design (é a chave "do navegador"), não confundir com a service_role key.
VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=coloque-a-anon-key-aqui

# URL do Cloudflare Worker responsável pelo OCR/extração do Pix nos boletos
# (ver src/lib/pixWorkerClient.ts). Se não definida, cai no fallback fixo
# hardcoded no próprio arquivo.
VITE_WORKER_URL=https://processo-de-pdf.erickramiro2010.workers.dev
